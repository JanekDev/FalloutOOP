package com.oopproject.world.locations;

public class WaterSource extends Source {
    public WaterSource(int x, int y, String name, int prey_replenishment_speed, int max_inside) {
        super(x, y, name, prey_replenishment_speed, max_inside);
    }
}

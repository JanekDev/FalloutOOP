package com.oopproject.world.locations;

public class FoodSource extends Source {
    public FoodSource(int x, int y, String name, int prey_replenishment_speed, int max_inside) {
        super(x, y, name, prey_replenishment_speed, max_inside);
    }
}
